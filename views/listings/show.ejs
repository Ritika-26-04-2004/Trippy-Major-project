<% layout('/layout/boilerplate') -%>
  <body>
    <h3>Listing Details :</h3>
    <div class="card listing-card offset-md-1 offset-lg-2 col-xs-12 col-sm-12 col-md-10 col-lg-8 ">
    <img
      
  src="<%= listing.image?.url || '/images/default.jpg' %>"
  class="card-img-top show-img"
  alt="Listing image"
/>

  
    
    <br />
    <ul>
      <li><i> owned by <%= listing.owner.username %></i></li> 
      <li><%= listing.title %></li>
  
      
      <li><%= listing.description %></li>
      <li>&#8377; <%= listing.price.toLocaleString("en-IN") %></li>
      <li><%= listing.location %></li>
      <li><%= listing.country %></li>
    </ul>
    <br />
   <!-- if(currUser && currUser._id.equals(listing.owner._id)) { %> -->
     <div class="btns">
      <a href="/listings/<%=listing._id %>/edit" class="btn btn-success col-2 offset-2"
        >Edit Details</a>
        
</form>

        
    <form method="POST" action="/listings/<%=listing._id%>?_method=DELETE">
      <button class="btn btn-dark">Delete Details</button>
   <!--  } %>  -->
    </form>
  </body>
